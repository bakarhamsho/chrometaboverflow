# ChromeDump Environment Variables

# REQUIRED: OpenAI API Key for generating tab summaries
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# OPTIONAL: Configure processing limits (uncomment to override defaults)
# Maximum number of words to read per tab (default: 100000)
# WORDS_LIMIT=100000

# Request timeout in milliseconds (default: 30000)
# REQUEST_TIMEOUT=30000

# Maximum retry attempts for failed requests (default: 3)
# MAX_RETRIES=3